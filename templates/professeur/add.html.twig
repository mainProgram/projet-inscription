{% extends 'base.html.twig' %}

{% block body %}

<legend class="mt-4">
    {% if editMode %}
        Inscription d'un professeur
    {% else %}
        Edition d'un professeur
    {% endif %}
    <a href="{{ path('professeur-list') }}" class="text-primary offset-5 text-decoration-none">Liste des professeurs</a>
</legend>

    {{ form_start(form)}}
    {# {{ form_row(form.prenom)}}
    {{ form_row(form.nom)}}
    {{ form_row(form.grade)}}
    {{ form_row(form.modules)}}
    {{ form_row(form.classes)}} #}
    {{ form_widget(form)}}

    {% if editMode %}
        <button class="btn btn-outline-primary">Ajouter</button>
    {% else %}
        <button class="btn btn-outline-primary">Editer</button>
    {% endif %}
    {{ form_end(form)}}

{% endblock %}

