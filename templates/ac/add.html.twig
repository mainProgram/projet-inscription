{% extends 'base.html.twig' %}

{% block body %}

<legend class="mt-4">{{title}}<a href="{{ path('ac-list') }}" class="text-primary offset-5 text-decoration-none">Liste des attachés</a></legend>

    {{ form_start(form)}}

    {{ form_row(form.prenom)}}
    {{ form_row(form.nom)}}
    {{ form_row(form.email)}}

    {% if editMode %}
        {{ form_row(form.password)}}
        {{ form_row(form.confirm_password)}}
        <button class="btn btn-outline-primary">Ajouter</button>
    {% else %}
        <button class="btn btn-outline-primary">Editer</button>
        {{ form_widget(form.password, { 'attr': {'hidden': 'hidden', 'value': ac.password} })}}
        {{ form_widget(form.confirm_password, { 'attr': {'hidden': 'hidden', 'value': ac.password} })}}
    {% endif %}
    {{ form_end(form)}}

{% endblock %}

